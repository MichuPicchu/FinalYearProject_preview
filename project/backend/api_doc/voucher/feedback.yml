read (non NULL) feedbacks and ratings from customers as a merchant
---
tags:
  - Voucher
parameters:
  - in: header
    name: rid
    schema:
      type: integer
      example: 1
    required: true
    description: restaurant id
consumes:
  - application/json
responses:
  200:
    description: a list of ratings and reviews
  400:
    description: return error message.
